import { useEffect } from 'preact/hooks'

export default function ({ username }: { username?: string }) {
  // useEffect(() => {
  //   window.location.href = username
  //     ? `https://nf.td/${username}`
  //     : 'https://nf.td'
  // })
  return null
}
